- hosts: all
  become: yes
  tasks:
    - name: Update package cache
      apt:
        update_cache: yes

    - name: Install apache2 package
      apt:
        name: apache2
        state: present

